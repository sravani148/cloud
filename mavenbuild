@Library ( 'Shared-Library')_

pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                script{
                  build()
                  }
            }
        }
    }
}
